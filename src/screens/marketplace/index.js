import React from 'react';
import {Text, SafeAreaView} from 'react-native';
import styles from './styles';

const Marketplace = () => {
  return (
    <SafeAreaView style={styles.container}>
      <Text style={styles.title}>This is Marketplace</Text>
    </SafeAreaView>
  );
};

export default Marketplace;
